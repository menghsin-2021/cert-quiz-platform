[tool.poetry]
name = "backend"
version = "0.1.0"
description = ""
authors = ["menghsin-2021 <lloivne4119@gmail.com>"]
readme = "README.md"
packages = [{include = "backend", from = "src"}]

[tool.poetry.dependencies]
python = "^3.8"
uvicorn = "0.22.0"
fastapi = "^0.115.12"
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
